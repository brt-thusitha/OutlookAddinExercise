<UserControl x:Class="OutlookAddin.Module.Appointments.Views.TitleView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:OutlookAddin.Module.Appointments.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Label Grid.Row="0"  HorizontalAlignment="Center" VerticalAlignment="Center" Content="Outlook Addin Application"  Foreground="Brown"  FontSize="35" FontFamily="Corbel"/>

        <TextBlock  Grid.Row="1"  Text="{Binding Title}" Margin="0,10,0,0" FontSize="14" Foreground="Blue"></TextBlock>
    </Grid>
</UserControl>
